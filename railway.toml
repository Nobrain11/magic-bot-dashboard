[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm --filter @workspace/db run push-force && node artifacts/api-server/dist/index.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "$MAGIC Bot API + Dashboard"
